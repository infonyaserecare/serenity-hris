# Running the prototype

```bash
npm install
npm run dev
```

Then open http://localhost:5177.

`hris-app.jsx` at the project root is the whole application — this folder only
holds the entry point that mounts it. Editing `hris-app.jsx` hot-reloads.
